import CSS from "./Header.module.css"
const Header = () => {
    return (
        <div className={CSS.container}>
            <div className={CSS.titleContainer}>
           <div className={CSS.title}>To do app</div>
            </div>
            <div>=</div>
        </div>
    );
};

export default Header;
